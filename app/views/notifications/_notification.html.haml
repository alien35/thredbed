%li
  %span{ :class => follow_num(notification.user) }= link_to index_profile_avatar_select(notification.user), link_through_path(notification)
  %span.notif_text= link_to "#{notification.notified_by.user_name} #{notification.notice_type}", link_through_path(notification)
  %br
