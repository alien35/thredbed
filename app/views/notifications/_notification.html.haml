%li.container
  %span{ :class => follow_num(notification.user) }= link_to index_profile_avatar_select(notification.user), profile_path(notification.notified_by.user_name)
  %span.notif_text= link_to "#{notification.notified_by.user_name} #{notification.notice_type}", link_through_path(notification)
  %hr.each_notif_hr
