-unless response.user == current_user
  #follow_form
    -if current_user.following?(response.user)
      = render 'responses/unfollow', response: response
    -else
      = render 'responses/follow', response: response

