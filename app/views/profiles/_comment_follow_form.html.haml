-unless comment.user == current_user
  #follow_form
    -if current_user.following?(comment.user)
      = render 'profiles/comment_unfollow', comment: comment
    -else
      = render 'profiles/comment_follow', comment: comment
