.item
  .well
    - if user_signed_in?
      = link_to (index_profile_avatar_select(post.user)), profile_path(post.user.user_name)
      %li.index_name=link_to post.user.user_name, profile_path(post.user.user_name)
    - else
      =index_profile_avatar_select(post.user)
      %li.signed_out_names=post.user.user_name
    =link_to (image_tag post.image.url(:medium)), post_path(post), method: :get
    %h3=post.title
    %h5=caption_max_length(post.commentary)
    %li=comment_count_index(post)
    %span Tags:
    %span=raw post.tag_list.map { |t| link_to t, tag_path(t) }.join(", ")
    %h6=time_ago_in_words post.created_at
    %hr=link_to post.link, post.link


