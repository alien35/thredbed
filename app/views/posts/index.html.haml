.container.organize_rows
  - @posts.each do |post|
    .item
      .well
        =link_to (image_tag post.image.url(:medium)), post_path(post)
        %h3=post.title
        =post.commentary
        =post.user.user_name
