<%= form_for(current_user.active_relationships.find_by(followed_id: @post.user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "post_follow_button" %>
<% end %>
